# Example environment variables. Copy to `.env` for local development.
# REMEMBER: anything starting with VITE_ is embedded into the public bundle.
# Never put private API keys or secrets here.

# Base path for the production build (must match the repository name).
BASE_PATH=/kana-quiz/
